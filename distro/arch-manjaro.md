# Arch Linux and Manjaro

Typical packages:

```bash
sudo pacman -S docker docker-compose freerdp util-linux git
sudo systemctl enable --now docker
```

Verify that `xfreerdp3` is provided by the installed FreeRDP package.

# Fedora and RHEL-family systems

Install Docker or Moby Engine, Compose v2, FreeRDP, `flock`, and X11 support using the current distribution documentation.

Typical package names include:

```bash
sudo dnf install freerdp util-linux git
```

The exact Docker packages and repository setup vary by release.

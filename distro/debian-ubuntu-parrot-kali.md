# Debian, Ubuntu, Parrot, and Kali

Typical packages:

```bash
sudo apt update
sudo apt install docker.io docker-compose-plugin freerdp3-x11 curl git
```

Package names vary by release. Check the available FreeRDP version and use your distribution's backports if necessary.
